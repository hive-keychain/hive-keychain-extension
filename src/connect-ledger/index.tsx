import React from 'react';
import ReactDOM from 'react-dom';
import ConnectLedger from 'src/connect-ledger/connect-ledger.component';

ReactDOM.render(<ConnectLedger />, document.getElementById('root'));

export {};
