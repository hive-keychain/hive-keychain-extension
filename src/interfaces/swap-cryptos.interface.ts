export interface SwapCryptosConfig {
  baseUrl: string;
  apiKey: string;
  headerKey: string;
}

export interface SwapCryptosCurrencyInfo {
  symbol: string;
  iconUrl: string;
  name: string;
  network: string;
}
