import config from 'src/__tests__/utils-for-testing/setups/config';
config.byDefault();
describe('auto-lock.component tests:\n', () => {
  it.todo('Must load autolock and match snapshot');
  it.todo('Must load stored autolock value and match snapshot');
  it.todo('Must set autolock, show message and goback to menu');
});
