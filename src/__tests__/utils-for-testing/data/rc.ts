import { RC } from '@interfaces/active-account.interface';

const rc = {
  delegated_rc: 1000,
  received_delegated_rc: 1000,
  max_rc: 10000,
  current_mana: 1000,
  max_mana: 10000,
  percentage: 100,
} as RC;

const fake_RC = { rc };

export default fake_RC;
