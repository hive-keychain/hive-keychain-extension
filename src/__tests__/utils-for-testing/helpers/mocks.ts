/**
 * This are the default elements within any ./mocks file in a test suite.
 * @param {object} constants Intended to define any variable we may need into the test suite.
 * @param {Function} beforeEach Async arrow function to handle beforeEach on the test suite.
 * @param {object} methods contains callable methods to work with: arrays, data processing.
 * @param {Function} extraMocks Arrow function to handle extra mock needed for the test suite.
 */
const helper = () => {};

export default { helper };
