import React from 'react';
import ReactDOM from 'react-dom';
import LinkLedgerDevice from 'src/ledger/link-device/link-device.component';

ReactDOM.render(<LinkLedgerDevice />, document.getElementById('root'));

export {};
